log:
  engine: zap
  zap:
    level: info
    caller: true
    stacktrace: true
    path:
      - /Users/Juminiy/go/src/kube/cmd/menud/log/app.log
    internalPath:
      - /Users/Juminiy/go/src/kube/cmd/menud/log/app_zero.log
  zero:
    level:
    path:

harbor:
  registry: http://harbor.local:80
  username: admin
  password: Harbor12345
  public: library

docker:
  host: tcp://docker.local:2375
  version: 1.43

kubernetes:
  kubeConfigPath: ~/.kube/config

minio:
  endpoint: 192.168.31.254:9000
  accessKeyID: minioadmin
  secretAccessKey: minioadmin
  secure: false